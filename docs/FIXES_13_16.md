# Исправления по замечаниям 13-16

Документ фиксирует изменения по дополнительным замечаниям 13-16.

## Таблица изменений

| № | Требование | Что сделано | Где проверить | Основные файлы |
| - | - | - | - | - |
| 13 | На `http://127.0.0.1:8081/users` сделать пользователей кликабельными, добавить редактирование пользователя, отобразить дату/время создания и обновления в таблице и на странице отдельного пользователя, добавить поиск в фильтре по дате/времени создания и обновления. | Строки пользователей стали кликабельными. Добавлена отдельная страница пользователя `/users/:login`. В таблице и карточке пользователя отображаются дата создания и дата обновления. Добавлено редактирование роли пользователя. Добавлены фильтры по логину, роли, дате/времени создания и дате/времени обновления. | `http://127.0.0.1:8081/users`, пример: `http://127.0.0.1:8081/users/admin` | `public/index.html`, `public/app.js`, `public/data.js`, `src/server.js`, `scripts/security-checks.js` |
| 14 | На `http://127.0.0.1:8081/table-actions` отобразить в таблице полезные поля, которые уже есть в фильтрах. | В таблицу действий добавлены колонки: таблица, действие, пользователь, создатель таблицы, ячейка, значение, дата создания таблицы, дата изменения таблицы, дата просмотра таблицы и дата действия. Глобальный поиск и фильтры учитывают эти поля. | `http://127.0.0.1:8081/table-actions` | `public/index.html`, `public/app.js`, `src/server.js` |
| 15 | Для `http://127.0.0.1:8081/table-actions` добавить страницу отдельного действия по аналогии с пользователями и сделать строки таблицы кликабельными. | Строки журнала действий таблиц стали кликабельными. Добавлена отдельная страница действия `/table-actions/:id`, где отображаются данные действия: таблица, действие, пользователь, владелец, ячейка, значение и даты. Сервер отдает приложение по прямому URL страницы действия. | `http://127.0.0.1:8081/table-actions`, пример: `http://127.0.0.1:8081/table-actions/spreadsheet-log-1` | `public/index.html`, `public/app.js`, `public/styles.css`, `src/server.js` |
| 16 | На `http://127.0.0.1:8081/import` после успешного импорта нужно показать количество импортированных элементов по каждому виду и остаться на странице импорта. | После успешного импорта выводится количество импортированных таблиц, форм, ответов, действий в таблицах и действий в формах. Автоматический переход на поиск/список таблиц убран, пользователь остается на странице импорта и видит результат операции. | `http://127.0.0.1:8081/import` | `public/app.js` |

## Проверки

После последней правки запускались:

- `node --check public/app.js`

- `git diff --check -- public/app.js`

- `docker compose up -d --build app`

- `npm test`

Результат: security smoke checks проходят.

