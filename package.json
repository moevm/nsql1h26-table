{
  "name": "tables-forms-arangodb-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "Prototype for storing, presenting, and analyzing tables and forms with ArangoDB.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "check:js": "node --check src/server.js && node --check public/app.js && node --check public/data.js && node --check public/spreadsheet.js",
    "check": "npm run check:js && git diff --check",
    "security:check": "node scripts/security-checks.js",
    "test": "npm run security:check"
  },
  "dependencies": {},
  "engines": {
    "node": ">=20"
  }
}
